apiVersion: v1
kind: Service
metadata:
  name: socialpublish-api
  namespace: socialpublish
spec:
  type: ClusterIP
  selector:
    app: socialpublish-api
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
